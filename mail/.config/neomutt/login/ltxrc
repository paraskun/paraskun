set mbox_type     = Maildir
set folder        = ~/.mail/ltx/
set spoolfile     = +Inbox
set trash         = +Trash
set header_cache  = ~/.cache/mutt/ltx

set from = ivan.paraskun@eltex.loc
set smtp_url = smtps://$from@mail.eltex.loc
set smtp_pass = `pass ltx/pass`
set smtp_authenticators = "login"

mailboxes +Inbox +Trash
